/**
 * ============================================================
 *  Utils — Fonctions transversales
 * ============================================================
 */

function log(msg) {
  console.log(`[GTARPCaisse] ${msg}`);
}